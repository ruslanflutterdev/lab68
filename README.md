# 🐊 Charades Game (Крокодил)

**Charades Game** — это многопользовательская мобильная игра на Flutter, где игроки должны объяснять и угадывать слова. Приложение поддерживает онлайн-режим, позволяя создавать игровые комнаты и присоединяться к ним с разных устройств.

## ✨ Основные возможности

* **🎮 Онлайн-мультиплеер**: Играйте с друзьями в режиме реального времени.
* **🏠 Создание игры (Host)**: Создайте новую комнату и получите уникальный код для приглашения друзей.
* **🔗 Присоединение (Join)**: Введите код комнаты, чтобы подключиться к существующей игре.
* **🎭 Выбор роли**: Система распределения ролей (ведущий/отгадывающий).
* **☁️ Supabase Backend**: Все действия в игре (ходы, очки, статус) мгновенно синхронизируются через Supabase Realtime.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Бэкенд**: [Supabase](https://supabase.com/) — используется для аутентификации, базы данных (PostgreSQL) и Realtime-событий.
* **Архитектура**: MVVM (Model-View-ViewModel).
    * **View**: Экраны и виджеты (`lib/features/charades/view`).
    * **ViewModel**: Логика состояния (`lib/features/charades/viewmodels`).
    * **Model**: Данные и репозитории.

## 📂 Структура проекта

Проект организован по принципу "Feature-first" (разделение по фичам):

```text
lib/
├── features/
│   └── charades/         # Основной модуль игры
│       ├── models/       # Модели данных (GameModel)
│       ├── services/     # Сервисы (SupabaseService)
│       ├── view/         # Пользовательский интерфейс
│       │   ├── game_screen.dart          # Экран игры
│       │   ├── game_setup_screen.dart    # Экран входа/создания
│       │   └── role_selection_screen.dart# Выбор ролей
│       ├── viewmodels/   # Управление состоянием (ChangeNotifier)
│       └── widgets/      # Виджеты (CustomButton, SetupForms)
└── main.dart             # Точка входа
